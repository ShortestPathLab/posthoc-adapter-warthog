{
  "name": "warthog",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "emmake make emcc -B && rm -rf ./build/emcc/lib ./build/emcc/obj",
    "publish": "gh-pages -d ./build/emcc -b wasm-bin -v ./build/emcc/Makefile -f"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
